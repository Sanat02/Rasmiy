databaseChangeLog:
  - changeSet:
      id: add token_reset_password for users
      author: Sanat
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: reset_password_token
                  type: varchar(255)

